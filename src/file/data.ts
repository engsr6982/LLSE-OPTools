import { pluginFolderPath } from "../utils/GlobalVars.js";

const __inits = {
    "config": {
        "version": "3.0.0",
        "language": "zh_CN"
    }
}

export let config;

export class dataOperation {
    static init() {

    }

    static load() {
    }
}