
export function kick_UI(player: Player): void {
    // const allPlayers = mc.getOnlinePlayers().filter()
    // const fm = Func_Module.CustomForm();
    // fm.addDropdown(Tr("kick.drop"), allPlayers, 0);
    // fm.addInput(Tr("kick.input"), "String", Config.get("Kick_Txt"));
    // player.sendForm(fm, (player, data) => {
    //     if (data == null) return Close_Tell(player);
    //     if (mc.getplayerayer(allPlayers[data[0]]).kick(data[1])) {
    //         player.tell(Gm_Tell + Tr("kick.succes.ok", allPlayers[data[0]]));
    //     } else {
    //         player.tell(Gm_Tell + Tr("kick.succes.err", allPlayers[data[0]]));
    //     }
    // });
}